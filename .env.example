# Claude API Configuration
CLAUDE_API_KEY=your_claude_api_key_here

# Database Configuration (for development)
# In production (Netlify), database is in /tmp
DATABASE_URL=data/demo.db

# Environment
NODE_ENV=development
