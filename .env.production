# Production build-time env for the Vite frontend (Cloud Run).
# These are CLIENT-SAFE values only — they are embedded in the public JS bundle:
#   - the Supabase publishable key is designed to be exposed to browsers (RLS enforces access)
#   - the API URL is the public Cloud Run service URL (same origin serves the SPA + API)
# NEVER put the Supabase secret/service key or any LLM key here — those are server-only
# and are set as backend env vars / Secret Manager on the Cloud Run service.
VITE_API_URL=https://vibe-cast-xijtfcuhyq-nw.a.run.app
VITE_SUPABASE_URL=https://ertsvhwtaeityanbmyzw.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_ODSEG_03DnIAyjqSW0-Bhw_uwj7Ihu5
