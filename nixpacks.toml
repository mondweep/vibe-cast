[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["rm -f package-lock.json", "npm install"]

[phases.build]
cmds = ["npm run build", "npm run build:server"]

[start]
cmd = "npm run start"
