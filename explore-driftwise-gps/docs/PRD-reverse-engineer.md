# Reverse Engineered PRD & Prompt Analysis

## 1. Documentation Analysis (The "Real" PRD)

After reviewing the `explore-driftwise-gps` codebase, it is clear that there isn't a single file named `PRD.md`. Instead, the product requirements and specifications are comprehensively documented across several files in the `docs/driftwise` directory.

The key documents that essentially function as the PRD are:

*   **Functional Requirements & Roadmap**: [`docs/driftwise/specifications/IMPLEMENTATION-ROADMAP.md`](../docs/driftwise/specifications/IMPLEMENTATION-ROADMAP.md)
    *   This file outlines the Executive Overview, detailed Phase Breakdown, user stories (implied capabilities), and success metrics. It serves as the primary functional specification.
*   **Technical Architecture**: [`docs/driftwise/architecture/AFD-ARCHITECTURE.md`](../docs/driftwise/architecture/AFD-ARCHITECTURE.md)
    *   This covers the technical requirements, component responsibilities, data flow, and system design.

## 2. Inferred Prompt & Generation Process

Based on the highly structured output, the specific use of **Domain-Driven Design (DDD)** concepts (Bounded Contexts, Aggregate Roots), and the presence of `CLAUDE.md` which references "Claude Flow V3" and "Automatic Swarm Orchestration", it is highly likely that this output was generated by a sophisticated agentic workflow or a very detailed "mega-prompt".

### Hypothetical Prompt Reconstruction

The following is a reconstruction of the prompt that likely generated this project structure:

> "Act as a Principal Software Architect and Product Manager. I want to build 'Driftwise', a voice-first Progressive Web App (PWA) that acts as a serendipitous local history companion for drivers using the Gemini Live API.
>
> Please initialize a project structure and comprehensive documentation suite. Specifically, I need:
> 1.  A **README.md** with a project overview, quick start, and 'Why' section.
> 2.  An **Architecture & Framework Document (AFD)** detailing the system components (React/Svelte, Capacitor, Gemini API), data flow, and security.
> 3.  A **Domain-Driven Design (DDD)** breakdown with Bounded Contexts (Location, Discovery, Voice, etc.).
> 4.  A detailed **8-Phase Implementation Roadmap** from setup to production release.
>
> Implementation details:
> *   Use SvelteKit or Ionic/Capacitor.
> *   No backend for V1 (local API keys, PWA local storage).
> *   Focus on 'serendipity' and 'historical facts' (avoiding generic tourism info).
> *   Include a 'Claude Flow' configuration structure for future agentic coding loops."

### Key Observations supporting this hypothesis:
*   **`CLAUDE.md`**: The existence of this file with specific "swarming" instructions suggests the user might have used a CLI tool like `npx @claude-flow/cli` to initialize the project, or explicitly requested an "Agentic-ready" setup.
*   **Structured Documentation**: The split between `AFD-ARCHITECTURE.md`, `DDD-OVERVIEW.md`, and `IMPLEMENTATION-ROADMAP.md` is a classic rigorous architectural pattern often used in advanced LLM prompts to ensure separation of concerns.
