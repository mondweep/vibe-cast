# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local credentials / runtime config — never commit
.env
.env.local
config.local.js

# Dependencies and build output
node_modules/
dist/
build/

# Python bytecode cache
__pycache__/
*.pyc

# Source book — copyrighted, must never be committed or deployed.
# Kept locally only, as a reference for tools/extract_curriculum.py.
PCM.pdf
*.pdf
